name: Development CI/CD

on:
  push:
    branches:
      - develop

env:
  AWS_REGION: ${{ secrets.AWS_REGION }}
  ECR_REPOSITORY: ${{ secrets.ECR_REPOSITORY }}
<<<<<<< Updated upstream
  CLUSTER_NAME: ${{ secrets.EKS_CLUSTER_NAME }}

jobs:
  deploy:
=======
  EKS_CLUSTER_NAME: multi-env-platform-cluster
  K8S_NAMESPACE: development
  DEPLOYMENT_NAME: multi-env-app
  CONTAINER_NAME: app

jobs:
  build-and-deploy:
>>>>>>> Stashed changes
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:

<<<<<<< Updated upstream
      - name: Checkout Repository
        uses: actions/checkout@v4

=======
      # 1. Checkout source code
      - name: Checkout Repository
        uses: actions/checkout@v4

      # 2. Configure AWS credentials
>>>>>>> Stashed changes
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

<<<<<<< Updated upstream
=======
      # 3. Login to ECR
>>>>>>> Stashed changes
      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

<<<<<<< Updated upstream
=======
      # 4. Build Docker image
>>>>>>> Stashed changes
      - name: Build Docker Image
        run: |
          docker build \
            -t $ECR_REPOSITORY:${{ github.sha }} \
            ./app

<<<<<<< Updated upstream
=======
      # 5. Tag Docker image
>>>>>>> Stashed changes
      - name: Tag Docker Image
        run: |
          docker tag \
            $ECR_REPOSITORY:${{ github.sha }} \
            ${{ steps.login-ecr.outputs.registry }}/$ECR_REPOSITORY:${{ github.sha }}

<<<<<<< Updated upstream
=======
      # 6. Push Docker image to ECR
>>>>>>> Stashed changes
      - name: Push Docker Image
        run: |
          docker push \
            ${{ steps.login-ecr.outputs.registry }}/$ECR_REPOSITORY:${{ github.sha }}

<<<<<<< Updated upstream
=======
      # 7. Install kubectl
      - name: Setup kubectl
        uses: azure/setup-kubectl@v4
        with:
          version: latest

      # 8. Configure kubectl for EKS
>>>>>>> Stashed changes
      - name: Update kubeconfig
        run: |
          aws eks update-kubeconfig \
            --region $AWS_REGION \
<<<<<<< Updated upstream
            --name $CLUSTER_NAME
      - name: Verify Cluster
        run: |
          kubectl config current-context
          kubectl get nodes

      - name: Verify Namespaces
        run: |
          kubectl get namespaces

      - name: Verify Deployments
        run: |
          kubectl get deployments -A

      - name: Update Deployment Image
        run: |
          kubectl set image deployment/multi-env-platform-app \
          multi-env-platform-app=${{ steps.login-ecr.outputs.registry }}/$ECR_REPOSITORY:${{ github.sha }} \
          -n development

      - name: Verify Rollout
        run: |
          kubectl rollout status deployment/multi-env-platform-app \
          -n development
=======
            --name $EKS_CLUSTER_NAME

      # 9. Deploy new image to Development
      - name: Deploy to Development
        run: |
          kubectl set image deployment/$DEPLOYMENT_NAME \
            $CONTAINER_NAME=${{ steps.login-ecr.outputs.registry }}/$ECR_REPOSITORY:${{ github.sha }} \
            -n $K8S_NAMESPACE

      # 10. Wait for rollout
      - name: Wait for Deployment
        run: |
          kubectl rollout status \
            deployment/$DEPLOYMENT_NAME \
            -n $K8S_NAMESPACE \
            --timeout=180s

      # 11. Verify deployment
      - name: Verify Deployment
        run: |
          kubectl get deployment \
            $DEPLOYMENT_NAME \
            -n $K8S_NAMESPACE

          kubectl get pods \
            -n $K8S_NAMESPACE
>>>>>>> Stashed changes
